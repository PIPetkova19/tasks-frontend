import { GpaGradePipePipe } from './gpa-grade.pipe-pipe';

describe('GpaGradePipePipe', () => {
  it('create an instance', () => {
    const pipe = new GpaGradePipePipe();
    expect(pipe).toBeTruthy();
  });
});
