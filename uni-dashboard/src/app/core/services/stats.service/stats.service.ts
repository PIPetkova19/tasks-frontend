import { Injectable, signal } from '@angular/core';

@Injectable({
    providedIn: 'root',
})

export class StatsService {
    /* TODO
    getWeeklyActivity(): { day: string; count: number }[]   // last 7 days
  getDepartmentBreakdown(): { department: string; count: number }[]
  */
}
